import { ethers } from 'ethers';
import { Server, ServerOptions } from './Server';
export declare class Registry {
    chainId: number;
    private contract;
    constructor(chainId?: number, walletOrProvider?: ethers.Wallet | ethers.providers.Provider);
    static getAddress(chainId?: number): string;
    getServers(quoteToken: string, baseToken: string, options?: ServerOptions): Promise<Array<Server>>;
}
