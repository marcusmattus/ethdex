import { ethers } from 'ethers';
import { LightOrder } from '@airswap/types';
export declare class Light {
    chainId: number;
    private contract;
    constructor(chainId?: number, signerOrProvider?: ethers.Signer | ethers.providers.Provider);
    static getAddress(chainId?: number): string;
    validate(order: LightOrder, senderWallet: string, signer?: ethers.Signer): Promise<Array<string>>;
    swap(order: LightOrder, sender?: ethers.Signer): Promise<string>;
    private convertToArray;
}
