import { ethers } from 'ethers';
import { Quote } from '@airswap/types';
export declare function createQuote({ signer, sender }: any): Quote;
export declare function isValidQuote(quote: Quote): boolean;
export declare function getTotalBySignerAmount(quotes: Array<Quote>): ethers.BigNumber;
export declare function getTotalBySenderAmount(quotes: Array<Quote>): ethers.BigNumber;
